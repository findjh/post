1、数字与字符串数字之间直接比较

      例如 数字5 与字符串数字'123'，这种比较先将数字字符串转为纯数字，然后为纯数字间的比较

2、字符串数字与字符串数字之间直接比较

      例如 ‘5’和‘123’比较，这种比较为ASCII码比较，依次取每个字符，字符转为ASCII码进行比较:‘5’>'123'  ==>true

3、字符串之间直接比较

      大小比较是依次取字符，字符转ASCII码，ASCII码先大的即为大