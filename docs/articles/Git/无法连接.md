推送分支时报：ssh: connect to host github.com port 22: Connection timed out
解决办法：

1. 在  ~/.ssh/config 文件新增以下内容

```sh
Host github.com
  Hostname ssh.github.com
  Port 443
```

2. 验证 

   ```sh
   ssh -T git@github.com
   ```

   

3. 重新推送